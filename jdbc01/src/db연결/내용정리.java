package db연결;

public class 내용정리 {
	// 1. JDBC 4단계 정리
	// >> 드라이버 설정 -> DB 연결 -> SQL문 객체 생성 -> SQL문 전송
	
	// 2. 데이터를 접근해서 처리하는 클래스를 부르는 이름
	// >> Data Access Object(DAO)
	
	// 3. DAO에 주로 넣는 기능 4가지
	// >> DML -> CRUD -> insert, select, update, delete
	
	// 4. DAO에 주로 넣는 기능중 C는?
	// >> create(insert문)
	
	// 5. SQL문으로 객체를 만들었을 때 값을 모르는 데이터를 표현하는 연산자는?
	// >> ?
}
